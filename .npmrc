# Use append-only reporter so pnpm doesn't buffer all child output before
# flushing. The default reporter OOMs on long workspace runs (lint/test)
# because it holds every line in memory until the run ends. Per-package
# scripts already bump their own --max-old-space-size; the outer pnpm
# wrapper is what was crashing.
reporter=append-only
